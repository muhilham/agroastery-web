[build]
buildCommand = "npm run build && cp -r .next/static .next/standalone/.next/static && cp -r public .next/standalone/public"

[deploy]
startCommand = "HOSTNAME=0.0.0.0 NODE_OPTIONS='--max-old-space-size=384' node .next/standalone/server.js"
